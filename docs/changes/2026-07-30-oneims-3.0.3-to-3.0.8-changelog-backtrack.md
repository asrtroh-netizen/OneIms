# OneIms 3.0.3 → 3.0.8 变更回溯（稳定性视角）

> 调研日期：2026-07-30  
> 范围：主产品 `app/` + `bridge/`（不含同仓并行演进的 `onetools` / `onebattery`）  
> 锚点：以 **release bump commit** 为准（见下表），**不以错位的 git tag SHA 为准**

## 版本锚点（真源）

| 版本 | versionCode | Release bump commit | 日期 | 本地 git tag |
|---|---|---|---|---|
| 3.0.3 | 73 | `bbd4354de`（另有同号覆盖包，见下文） | 2026-07-25 | **无** |
| 3.0.4 | 74 | `3146d2538` | 2026-07-25 | **无** |
| 3.0.5 | 75 | `447514abe` | 2026-07-26 | `v3.0.5` ✅ 对准 bump |
| 3.0.6 | 76 | `0b4b0b588` | 2026-07-29 | `v3.0.6` ❌ 指向 `96b993317`（3.0.5 README） |
| 3.0.7 | 77 | `b5d10b2b0` | 2026-07-29 | `v3.0.7` ❌ 指向 `28e49ee01`（README） |
| 3.0.8 | 78 | `7c03c5e00`（README 同步 `d44134f1b`） | 2026-07-30 | `v3.0.8` ❌ 指向 `e8a0b08a8`（3.0.7 README） |

补充：

- GitHub Release 现有：`v3.0.5` ~ `v3.0.8`；`v3.0.3` / `v3.0.4` **当前查不到 release**（历史文档仍写过 3.0.3 upload）。
- APK 资产与 tag 目标 commit 可能不一致：应用以 **versionCode + 上传的 APK** 为准；源码对照请用上表 bump SHA。

## 3.0.3 为何常被说「稳」

覆盖发包说明见 `docs/changes/2026-07-25-release-3.0.3-overwrite-sandbox.md`：

- 主线小：持久能力探测 + 强制临时写入（默认关）+ 沙盒持久旁路（**默认关**）
- 现场反馈：覆盖安装后「重启之后可以生效」
- **没有**：系统更新屏蔽、默认开沙盒旁路、OneKuku V15 级开机韧性重写、国产 VoWIFI 统一门控等大面改动

## 分段变化（相对上一 release bump）

### 3.0.3 → 3.0.4（+1035 / -15，风险面最大）

主线（README What's New 一致）：

1. **首页「持久性 VoLTE/NR」**：沙盒旁路 **默认开**（相对 3.0.3 覆盖包「默认关」是关键语义翻转）
2. **VoNR 写入 NR 可用性**：便于系统设置露出 VoNR
3. **系统更新屏蔽（独家，默认开）**：组件禁用 + settings + Magisk hosts 挡 OTA（best-effort）
4. 另含：VOIMS opt-in key 68、沙盒 Instrumentation / Provider 整套落地

稳定性含义：默认行为从「少动手」变成「尽量真持久 + 尽量挡更新」——权限/组件侧副作用面明显变大。

### 3.0.4 → 3.0.5（+163 / -1053，产品面收敛）

1. 非 Tensor（含联发科等）**放开 VoWiFi 写入尝试**（是否上栏仍看 OEM）
2. 独家页去掉三项显示/切卡入口，保留身份/国家码/守护/APN/专家编辑
3. 净删大量 Experimental 展示逻辑（行数减少主要来自这里）

稳定性含义：写入门更宽（国产机更敢写），但 UI 入口收敛。

### 3.0.5 → 3.0.6（+1609 / -494，改动量最大）

主线：

1. 首页紧凑 + 通道 Hero **三态**（未激活 / 激活中 / 就绪）
2. **OneKuku 对齐 Shizuku V15 开机韧性**：配对码进 FGS、binder 重试、Wi‑Fi/解锁续跑、可选 Watchdog（默认开）
3. IMS：一加等对 key 26/27 拒写 **软成功**；高通兼容从「一刀切不支持」降级为可尝试
4. 新增 `ProvisioningWritePolicy` 等写入策略层

稳定性含义：方向是「更抗崩 / 更抗掉线」，但 OneKuku 后台面（FGS/Watchdog/Receiver）显著变复杂——这是 3.0.3→3.0.8 最大结构跃迁。

### 3.0.6 → 3.0.7（+110 / -85，稳定性补丁）

1. 修 OneKuku「假就绪」与一加「点应用闪退」
2. 修首页 StatusHero 标题被胶囊吃字

稳定性含义：针对 3.0.6 回归的小修，风险低。

### 3.0.7 → 3.0.8（+814 / -49，兼容/排障）

1. Pixel VoWIFI / 通信开机自启定为 P0 硬保证；国产 OEM VoWIFI **统一门控**（含三星/荣耀）
2. Broker：`persistent=true` 拒写同会话降临时（对齐 pixel-volte-patch，防闪退）
3. 小米等拒写软化；排障页「导出详细日志」（`DiagFileLogger`）
4. Pixel VoLTE 硬、VoIMS opt-in 可 soft-fail

稳定性含义：偏防崩与可观测；但「软成功 / 降临时」会让体感成功 ≠ 重启后仍持久——排障时要对照诊断标签。

## 全量代码面（`bbd4354de`..`7c03c5e00`）

- `app/` + `bridge/`：`60 files, +3697 / -1652`
- 默认偏好（3.0.8 `ConfigStore`）：
  - `sandbox_persist_bypass` **默认 true**（3.0.3 覆盖包为 false）
  - `system_update_shield` **默认 true**（3.0.3 无此能力）
  - `force_temporary_override` 仍默认 false
  - `onekuku_watchdog` 默认 true（3.0.6 起）

## 稳定性对照结论（给「想回到 3.0.3 手感」）

若用户觉得 3.0.3 更稳，优先怀疑这三刀（按爆炸半径）：

1. **3.0.4 默认开沙盒持久 + 默认开更新屏蔽**（系统组件/设置/hosts）
2. **3.0.6 OneKuku V15 级后台韧性重写**（FGS / Watchdog / 多 Receiver）
3. **3.0.5~3.0.8 写入策略软化与 OEM 门控**（少闪退，但成功语义变宽）

3.0.7 / 3.0.8 多数是在上述大面上做崩溃与假态修补，而不是回到 3.0.3 的「小面默认关」模型。

## 验证命令（本轮已跑）

```text
git log --oneline bbd4354de..7c03c5e00 -- app/ bridge/
git diff --shortstat bbd4354de..7c03c5e00 -- app/ bridge/
git diff --stat <prev>..<next> -- app/ bridge/   # 各版本分段
git tag -l "v3.0*"
gh release list -L 10
rg "getBoolean\\(KEY_SANDBOX|getBoolean\\(KEY_SYSTEM_UPDATE" app/src/main/java/com/oneims/app/core/ConfigStore.kt
```

未跑：真机回归 / APK 二进制 diff（NOT RUN：无指定机型与 3.0.3 APK 本地对照包）。
