# 变更说明 · 功能页强度 / 独家格样式拆分

日期：2026-07-13  
范围：能力页 + 独家页

## 按用户澄清落地

1. **功能页**：恢复 CarrierIMS「5G信号强度调整」开关，并入五连排；点【应用 5G】写入。
2. **独家页**：恢复截图「信号格显示样式」三选一 +「应用信号格显示样式」。
3. **共用偏好**：`SignalBarDisplayMode`；两边开关/选项同步；4/5 系统写入仍为同一 CarrierIMS SSRSRP（文案继续披露不保证总柱数）。
