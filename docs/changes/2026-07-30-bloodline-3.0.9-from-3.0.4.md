# 2026-07-30 · 血统纯重建 3.0.9（自 3.0.4）

> **架构真源（底包冻结）**：见 [`docs/architecture/2026-07-30-oneims-3.0.4-base-package.md`](../architecture/2026-07-30-oneims-3.0.4-base-package.md)。本文保留首次血统重建的落地与验证记录。  
> **命名更正（2026-07-30）**：本地产品号 **3.0.9 已废止**；当前构建身份回到底包 **3.0.4 / vc74**（见 `2026-07-30-abolish-local-3.0.9.md`）。下文「3.0.9」仅指当时重建实验的历史称呼。

## 决策

用户选定 **方案 B**：以 **3.0.4 (`3146d2538`)** 为稳根，只叠加精选好补丁，**不继承** 3.0.5～3.0.7 脏开门。

- 分支：`bloodline-3.0.9`（历史分支名；产品号不再叫 3.0.9）
- 当时实验版本号：`3.0.9` / `versionCode 79` → **已废止**，现回 `3.0.4` / `74`
- **不**创建 GitHub Release、不上传 APK

## 明确排除（脏）

| 项 | 来源 | 处理 |
|---|---|---|
| 非 Tensor VoWiFi 软件开门 | 3.0.5 `ce1130ae7` | 未 cherry-pick |
| 体检非 Tensor → DEGRADED「可尝试」 | 3.0.6 `afdbda2cc` 策略部分 | 已改回 `!tensor → UNSUPPORTED` |

## 叠加（好）

- 三态通道 / 矮屏紧凑 / 假就绪硬门禁 / Hero 吃字（拣自 3.0.6～3.0.7 UI）
- 小米 soft-fail + 诊断日志 + 国产 VoWIFI-only / temporary-first（终态移植）
- 前台桥重连、杀后台假状态清理、Toast 降噪
- R2 砍三项独家运行时路径

## 落地说明（诚实）

逐条 cherry-pick 在 `ImsController` / `ProvisioningWritePolicy` 等处冲突过多。  
为可编译交付：在 `3146d2538` 根上拣入 UI 后，将 `app/` **对齐 main 终态**，再**单独锁回** 3.0.4 `CompatChecker` 纪律。  
因此：**门禁血统 = 3.0.4**；功能树 ≈ 优化版 3.0.9 终态 − 非 Tensor「可尝试」。

## 验证（本轮）

```text
git merge-base --is-ancestor ce1130ae7 HEAD  → exit 1（脏开门不在血统内）
CompatChecker: !tensor -> SupportLevel.UNSUPPORTED
./gradlew :app:compileOnelinkDebugKotlin :app:compileOnekukuDebugKotlin  → BUILD SUCCESSFUL
./gradlew :app:testOnekukuDebugUnitTest --tests CompatAndSocPolicyTest \
  --tests OemDeviceCompatTest --tests ProvisioningWritePolicyTest \
  --tests CarrierNameFormatTest  → BUILD SUCCESSFUL
versionName=3.0.9 / versionCode=79
```

- 真机装包：NOT RUN（本轮未要求）
- 附带修复：`formatCarrierShortName` 随三项独家删除后调用点残留，已拆到 `CarrierNameFormat.kt`（main 尖端同缺，建议回灌）

## Git 合并（2026-07-30）

已执行：git merge --no-ff bloodline-3.0.9 合入 main。  
冲突：onetools/.../TileHelper.kt（血统侧删除 vs main 改名保留）→ **保留 OneTools 切卡助手**（独家能力在 OneTools）。
