# 首页：运营商+我的配置 开关 → 应用

## 变更

原「临时 Root 持久化改运营商」**开关**易误解（开/关 vs 立刻写）。  
改为行动作 **「应用运营商与我的配置」**：一点即 `TempRootPostSuccessActions`（参考 XML + 重放我的配置）。

无可用 su 时明确提示先跑一键临时 Root，避免笼统报错。

版本 **3.2.6 / 87**。

## 3.2.7 显隐

「应用运营商与我的配置」改回 **仅 `showRootFeatures`（有可用 Root）时显示**，不与一键临时 Root 常显同级——按隐藏功能处理。
