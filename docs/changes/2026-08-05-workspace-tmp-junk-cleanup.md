# 工作区临时探针垃圾清理

## 背景

本地根目录堆积大量 Agent/运维探针产物（`.tmp-*`、`tmp-*`、截图、诊断输出、根目录 APK），
`git status` 看起来像整仓脏乱；经核对，`origin/main` **未被这些文件污染**（均为未跟踪本地文件）。

## 变更

- 删除本地未跟踪临时文件/目录（约 280 项），保留 `README.md` 与正式工程源码
- `.gitignore` 增补：`.tmp-*`、`.tmp_*/`、`tmp-*`、`tmp_*/`、`.wt-*/`，防止复发

## 未改动

未触碰既有业务改动（如 `HomeScreen.kt` / `app/build.gradle.kts` 等未提交工作）。
未对远程做「整库只留 README」的毁灭性重置。
