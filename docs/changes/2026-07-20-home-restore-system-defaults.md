# 变更说明：首页一键恢复系统默认状态

**日期**：2026-07-20  
**范围**：首页「快速开始」快操作区；复用既有 `SafetyGuard.restoreDefaults`。

## 行为

- 新增危险色按钮「一键恢复系统默认状态」，与「保存配置 / 一键应用上次配置」并列。
- 点击后弹出确认框；确认后清空当前 SIM 的 CarrierConfig 覆盖及相关显示增强，恢复运营商系统默认。
- 不删除已保存的通话配置快照（仍可用「一键应用上次配置」写回）。
- 前置：已选卡、OneKuku/OneLink 已授权、无其它操作进行中。

## 验证

- `compileOnekukuDebugKotlin` / 资源合并
- 真机确认路径：人工清单
