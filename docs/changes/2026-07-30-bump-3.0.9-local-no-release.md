# 2026-07-30 · 本地升号 3.0.9（不发包）

## 决策

- 优化后构建命名 **`3.0.9` / versionCode `79`**
- **不**创建 GitHub Release、**不**上传 APK、**不**移动 `v3.0.8` tag
- 公开 README 下载区继续指向已发版 **3.0.8**

## 改动

- `app/build.gradle.kts`：`3.0.8`/`78` → `3.0.9`/`79`
- README：新增 What's New · 3.0.9（标明未发 Release）
- 纠正 R2 文档中「仍叫 3.0.8」的误记

## 验证

```text
Select-String app/build.gradle.kts -Pattern "oneImsVersionName|versionCode"
→ 3.0.9 / 79
```

发包 / 真机安装：NOT RUN（用户要求不发包）。
