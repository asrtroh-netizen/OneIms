# 首页底栏设备卡美化 + 排障/日志弹出试方案

## 需求合集

1. 设备详情框放到首页**最下面**
2. 审美与排版讲究好看
3. 排障页原四个只读检查 → 全部弹出确认试方案
4. 日志改为弹出式查看

## 落地

- `HomeScreen`：`DeviceDetailsCard` 移至列表末项；按 `label: value` 分行展示，主色标签 + 正文，可展开
- `DiagnosticsScreen`：健康/ePDG/IMS/配置 dump 与重应用/导出同一套 `AlertDialog` 确认；日志改为「查看诊断日志」行 → 弹窗内可选中文本 + 复制/清空

## 验证

见交付总结编译结果
