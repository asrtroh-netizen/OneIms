# OneSo 编译工厂 CLI

- 路径：`tools/oneso/oneso.py`
- 命令：`list` / `info` / `build` / `install` / `import-so`
- 包 IonStack `Makefile`；`import-so` 用于已验证成品（comet 0705 脚手架尚不能 make）
- 冒烟：`oneso list` → 31 projects；`import-so comet-CP2A.260705.006` 已更新 catalog
