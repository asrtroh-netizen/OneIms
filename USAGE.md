# OneIms 真机落地指南（照着做即可）

> 目标：把 OneIms 装上你的 Pixel（如 Pixel 9 Pro Fold），一步步开好 VoLTE/VoWiFi，出问题也能自救。
> 铁律：电话/数据/短信最高优先——任何一步觉得通信不对劲，先按【一键还原】。

---

## 第 0 步 · 安装

1. 把 `app/build/outputs/apk/debug/app-debug.apk`（或命名包 `OneIms-2.0.0-android17.apk`）传到手机，点击安装（首次需允许「安装未知应用」）；本轮版本应显示 **2.0.0**。
2. 打开 OneIms，首启会申请「电话权限」，请允许（用于读 SIM、判断 IMS 状态）。

> 这是 debug 签名版，仅自用/测试；要长期用或分发，找我出 release 签名版。

---

## 第 1 步 · 装好并启动 Shizuku（关键前置）

OneIms 靠 Shizuku 免 root 提权。先装 [Shizuku](https://shizuku.rikka.app/)，再启动它。

### 出门没 WiFi 也能启动（破除误区）

无线调试连的是**手机本机 127.0.0.1**，不需要连 WiFi 上外网。在 OneIms 排障页「Shizuku 设置」分组有四条自救：

- **【一键打开 Shizuku 配对】**：直接拉起 Shizuku，在它的「通过无线调试启动」里输配对码；
- **【打开无线调试】**：跳到系统无线调试/开发者选项页；
- **【开热点兜底】**：部分机型无线调试需要本地网络接口，**开个人热点即可满足（不用真联网）**；
- **【复制免电脑命令】**：给出 Termux + 本机 adb 对 `localhost` 自配对的现成命令。

> 配对码是系统随机生成、只显示在系统 UI，任何 app 都读不到，**必须你手输一次**；配对成功后一般可免电脑重连。

---

## 第 2 步 · OneIms 授权 + 兼容性体检

1. 回到 OneIms 首页「快速开始」→【授权】→ 弹窗选「始终允许」。
2. 点【刷新】，在「设备详情」中查看型号/系统/补丁/是否 Tensor/SIM 数/绕过策略。
3. 点【体检】看结论：
   - **完全支持** → 放心用；
   - **降级可用** → 能用，但重启后大概率需重开（靠守护自愈）；
   - **需先配 Shizuku** → 回第 1 步；
   - **不支持**（非 Tensor 等）→ 不建议使用。

---

## 第 3 步 · 开 VoLTE/VoWiFi（推荐走「运营商推荐」）

**最省心**：能力页「移动网络」选中你的卡 → 看它自动识别的运营商与推荐 →【按推荐一键开启】。

**想手动**：切到「能力」页 → 勾选 VoLTE / VoWiFi / VoNR → 选 WFC 模式（默认「蜂窝优先」最稳）→【应用核心能力】。

**5G 与信号强度**：同页「运营商能力与 5G」五开关连排（VoLTE/VoWiFi/VoNR/5G NR/5G信号强度）。信号强度对齐 CarrierIMS：只写 `5g_nr_ssrsrp_thresholds_int_array=[-128,-118,-108,-98]`，且需同时开启 5G NR 后点【应用核心能力】才会写入；关闭信号强度并应用会恢复当前卡的 SSRSRP 基线。它不增强真实信号，也不保证改变状态栏总柱数。

> VoLTE/VoWiFi/VoNR 核心能力与 5G/信号强度统一由【应用核心能力】写入（`persistent=false` + 通信健康检查，异常时自动回滚）。信号强度走写后回读与基线恢复，不宣称增强真实信号。

---

## 第 4 步 · 到系统设置打开开关 + 验证

1. 系统「设置 → 网络和互联网 → SIM」→ 打开 **VoLTE / 4G 通话** 与 **WiFi 通话**（此时开关应已出现）。
2. 直接在 OneIms 排障页点【诊断】：会按 SIM 1/SIM 2 分别显示 VoLTE/VoWiFi 可用态与 **IMS 注册: 已注册（LTE/IWLAN/NR）**，无需再手敲 `*#*#4636#*#*`；想看底层配置到底写没写进去，点【配置查看】dump 当前 CarrierConfig 关键项。
3. 已开能力但仍未注册时，先点【重新注册 IMS】安全重建当前卡槽注册链；它不会切飞行模式或改首选网络，但会短暂注销 IMS，**通话中不要执行**。
4. 仍有问题时打开【离线 APN 库】：右上角选卡胶囊是唯一写入目标，可按中英文国家名、ISO、MCC、MNC、PLMN、运营商、APN 或 IMS 类型搜索。浏览和复制完全离线；只有标记为 IMS、且匹配选中 `subId` 的专用模板才能进入修复确认。
5. 打个电话确认不掉 3G、发条短信确认能收发。

> 当前库内置 6,712 条候选（628 条含 IMS），来源与版本随 APK 固定。APN 会因套餐、MVNO、漫游和地区变化；找不到完全匹配时，优先联系运营商，不要为了“试试看”覆盖普通上网 APN。

## 第 5 步 · 按需使用高级能力

- **能力页 · 运营商显示与漫游选项**：提供漫游 WFC、WiFi-only、系统模式入口、VoWiFi/数据制式图标、4G/LTE+ 标签、IMS 状态与增强型 4G 等 Pixel IMS 同类能力；改完点【应用高级选项】；
- **排障页 · 完整配置导出**：只读导出当前 SIM 的全部 CarrierConfig 到排障日志，适合对比运营商配置；
- **实验功能页 · 受限专家编辑器**：只允许写当前配置中已存在、类型可识别且不归显示管理器专管的键，并拦截网络选择、紧急通信和卫星相关键。不了解键含义时不要试；
- **系统快捷设置**：编辑快捷设置面板，可添加【IMS 状态】和【VoLTE 配置】两枚磁贴。前者只读刷新注册态，后者明确重应用最近一次保存的配置。

这些能力按 Pixel IMS 的公开可观察行为独立实现，没有复制其 GPL-3.0 源码。

---

## 第 6 步 · 开「IMS 掉线守护」（治间歇掉线）

进入「实验功能」→ 打开【掉线守护】。

- 开机后你**启动一次 Shizuku**，守护会在其就绪瞬间自动重开配置；
- 每 2 分钟巡检，发现 IMS 掉了自动重开——治「家里不行、到单位才行」。

> **“重应用”不是重复安装应用。** OneIms 的 CarrierConfig 覆盖固定使用 `persistent=false`，重启手机或 Shizuku 后可能被系统清掉；重应用只是把最近一次成功的核心配置和高级选项再幂等写一遍。排障页会显示最近触发原因、时间和结果。守护要生效，Shizuku 必须在跑（非 root 天花板）。

---

## 常见问题速查

| 现象 | 先做什么 |
|---|---|
| 能上网但打不通电话 | 先【诊断】→【重新注册 IMS】；仅 Android 17 已开能力仍不注册时，在【离线 APN 库】选当前 SIM 的专用 IMS 模板再修复 |
| VoWiFi 开关有但连不上 | 排障页【ePDG 自检】：解析失败=运营商没放通；端口不通=换个 WiFi |
| 重启后 VoLTE 没了 | 启动 Shizuku（守护会自动重开），或再点一次一键应用 |
| 提示“重应用最近配置” | 不是重装 App；它只重放最近保存的临时 CarrierConfig。到【排障】页可查看触发原因和结果 |
| 状态栏图标没有立刻变化 | 部分 System UI 会缓存图标策略；先锁屏/解锁，仍不刷新再重启手机 |
| 电话/短信/上网出问题 | 首页【一键还原】清空覆盖和已保存重放方案、恢复运营商默认（必要时重启一次） |
| 换网络后掉注册 | 先用【重新注册 IMS】；仍失败再考虑飞行模式或等待守护巡检 |
| 运营商名称写错 | 实验功能页点【恢复运营商名称】，确认系统默认名恢复后再重新填写 |
| 出现 RETRIEVE_WINDOW_CONTENT / 大段白色报错 | 升级到 **1.2.2 或更高版本**；新版不再连接 UiAutomation，并修复连续操作残留代理，重启 Shizuku 后重试 |
| 提示 WRITE_SECURE_SETTINGS | 确认 OneIms 为 **1.2.2 或更高版本**，更新 Shizuku、重新授权后重试；旧权限代理路径无效 |
| “写入失败”需要完整原因 | Snackbar 只给短提示，完整 Binder/反射异常保留在【排障】日志，复制后再反馈 |

---

## 边界（诚实）

- 能否真打通最终取决于**运营商是否放通** VoLTE/VoWiFi/ePDG；
- 新补丁下 **VoWiFi 部分能力可能需 root**；
- AOSP 允许混合 APN type；【APN 修复】来自尚未合并的 Android 17 社区实测方案，不是通用必做项，因此不会后台静默执行；
- 离线 APN 库是 Apache-2.0 开源数据的本机候选索引，不依赖网络，也不保证运营商已为你的号码/套餐放通；MVNO 不确定项只允许查看/复制；
- APN 优先按当前订阅修复；系统只有运营商全局模板时，同 MCC/MNC 的 SIM 会共享这次拆分；
- 核心能力只做「加法」，**绝不改首选网络类型、绝不禁蜂窝**；APN 修复是唯一会移动既有 `ims` type 的路径，需确认且失败会回滚。
