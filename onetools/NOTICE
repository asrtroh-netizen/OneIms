# Third-party notices for OneTools

## Pixel Meter (network speed core)

- Project: https://github.com/Mystery00/PixelMeter (also Pixel-Tailor-CN/PixelMeter)
- License: Apache License 2.0
- Use in OneTools: physical-interface traffic sampling that ignores VPN transports
  (see `com.onetools.app.meter.PhysicalSpeedSampler`), adapted from Pixel Meter's
  `SpeedDataSource` approach with attribution.

## Battery Info (capability reference only)

- Project: https://github.com/DarkJoker360/BatteryInfo-app
- License: Apache License 2.0
- Use in OneTools: inspired the *set of public battery fields* shown in UI.
  OneTools `BatteryReader` is a clean-room implementation against Android
  `BatteryManager` / `ACTION_BATTERY_CHANGED` public APIs — not a copy of
  Battery Info source files.

## Obtainium (bundled install path)

- Project: https://github.com/ImranR98/Obtainium
- License: GNU GPL-3.0
- Use in OneTools: the Update Center can download the **official Obtainium APK**
  from GitHub Releases and hand it to the system package installer.
  Obtainium remains a **separate program**; its Flutter source is **not** merged
  into OneTools. Source offer: the GitHub repository URL above.
  Redistributing the Obtainium APK does not place OneTools Kotlin sources under GPL,
  provided Obtainium stays a separate work (arm's-length Intent/install).
