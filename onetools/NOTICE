# Third-party notices for OneTools

## Battery Info (capability reference — clean-room)

- Capability set referenced for the read-only battery screen.
- License: Apache License 2.0 (typical for Battery Info–style FOSS apps).
- OneTools battery reader is first-party clean-room code; see `battery/BatteryReader.kt`.

## Obtainium / GlassWire / Pixel Telo

- **Out**: update-center presets that pull Pixel Telo / forks; GPL Obtainium source.
- Update center remains first-party (does not embed Obtainium source).

## OneCaller (first-party) + geo

- CallScreening + Contacts Directory are first-party clean-room.
- Mobile geo uses schema `onetools.geo.v1` JSON (`assets/caller/geo_v1.json`).
- **Do not** bundle GPL `phone.dat` (e.g. xluohome/phonedata) into `:onetools`.

## Pixel Meter (capability reference — clean-room / Apache-inspired)

- Overlay / notification speed meter is first-party adaptation; see meter package.
